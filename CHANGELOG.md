# 変更履歴

リリースノートはここから生成する(`.github/workflows/release.yml`)。
**最新版を一番上に置く。** 見出しは `## vX.Y.Z` の形にすること。

## v1.0.1

### 直った不具合

- **「ぼかし背景」レイアウトで切り抜きを書き出せなかった。**
  縦横変換で「ぼかし背景」を選ぶ(または顔検出が失敗して自動で切り替わる)と、
  書き出しが `No such filter: 'boxblur'` で失敗していました。背景ぼかしに
  使っていたフィルタが同梱ffmpegに含まれない種類だったためで、同等の別方式に
  置き換えて解決しました

### 変わったこと

- **macOS版に署名と公証(notarization)を付けました。** これまでは
  ダウンロード後に「壊れているため開けません」と表示され、ターミナルでの
  回避操作が必要でしたが、そのまま開けるようになりました

## v1.0.0

### 変わったこと

- **macOSに対応しました。** Apple Silicon の Metal で whisper.cpp が動き、
  .dmg で配布します。これで Linux / Windows / macOS の3OSすべてに対応し、
  v1.0.0 としました
- **macOS版にffmpegを同梱しました。** Homebrewのffmpeg 9は字幕焼き込み(libass)が
  外されていて、別途インストールしても書き出しが必ず失敗するためです。
  Windows版と同じく、使う機能だけ(libass+VideoToolbox)のLGPLビルドを同梱します
- **実行環境を初回起動時に1回だけ検出して固定するようにしました。**
  これまでは文字起こしのたびにGPUやライブラリを調べ直し、そのときの状況で
  エンジンが変わっていました。条件が増えるほど「設定と実際の動きが合わない」状態になり、
  環境によっては文字起こしが失敗していました。
  これからは、初回起動時にお使いの環境(OSとGPU)を判定して構成を確定し、
  以後は毎回まったく同じ動作をします。設定タブの「実行環境」で確定した内容を確認でき、
  GPUを増設したときは「再検出」で更新できます
- **GPUがある機体は、メーカーを問わず whisper.cpp で動くようになりました。**
  NVIDIA / AMD / Intel は Vulkan、Apple Silicon は Metal で動きます。
  **CUDA や ROCm の導入は不要**になり、GPUドライバだけで動きます
- **文字起こしが始められないときは、はっきり失敗して直し方を出すようにしました。**
  以前は黙ってCPUに切り替わり、数十分待たされたうえに理由が分かりませんでした
- **設定画面を作り直しました。** 選んでも効かない項目(ASRエンジン・話者分離エンジン・
  VRAM割当)を削除し、「文字起こし / 整形 / 字幕 / レイアウト / LLM」に整理しました。
  実行環境の情報は設定タブの先頭にまとめました
- **アプリの構成が小さくなりました。** 使わなくなったAIライブラリ(PyTorch)を
  開発環境からも外し、配布物と同じ構成で開発・テストするようにしました

## v0.11.1

### 直った不具合

- **CUDAライブラリの判定漏れで文字起こしが失敗することがあった。**
  pip配布のライブラリ(nvidia-cublas等)は「ファイルがある」だけでは
  faster-whisperから読み込めない環境があり、GPU実行を試みて
  「Library libcublas.so.12 is not found」で落ちていました。
  判定時にライブラリを実際にプロセスへ読み込むよう変更し、
  読み込めた場合はそのままGPUで動くようになりました
  (読み込めない場合は従来どおりwhisper.cpp(Vulkan)またはCPUで完走します)

## v0.11.0

### 変わったこと

- **文字起こしの進捗に工程名が出るようになりました。**
  「音声を読み込み中→話者分離中→文字起こし中→保存中」と、いま何をしているかが
  分かります。これまではずっと「文字起こし中」で、先に走る話者分離が
  止まっているように見えていました
- **話者分離の進捗バーが実際の処理に合わせて動くようになりました。**
  これまでは話者分離のあいだ進捗が止まっていました(長尺では数分)
- **LLMプロバイダを選ぶ場所でAPIキーを登録できるようになりました。**
  未登録のクラウドLLMは選択肢に「(APIキー未設定)」と出るだけで、
  どこで登録するのか分かりませんでした。選ぶその場に登録欄を出し、
  選択中のプロバイダは登録済みでも差し替え・削除ができます
  (プロジェクト設定・新規プロジェクト画面にも同じ動線が付きます)

## v0.10.1

### 直った不具合

- **Geminiの新形式APIキー(AQ.〜)が登録できなかった。** 接頭辞(AIza〜)での
  形式チェックをやめました。代わりに登録時に提供元のAPIへ接続してキーの有効性を
  確認し、通ったものだけを保存します(Claudeも同様に確認します)
- **NVIDIAドライバのみの機体(CUDAランタイム無し)で文字起こしが必ず失敗していた。**
  「Library libcublas.so.12 is not found」で落ちるため、必要なライブラリが
  見つからないときはwhisper.cpp(Vulkan)またはCPUで完走するようにしました。
  環境パネルにも理由を表示します
- **Ubuntuのダークテーマで設定画面のプルダウンの文字が見えなかった。**
  ページの配色(color-scheme)をフォーム部品に伝えていなかったため、
  ダークUIでも中身がライト配色で描かれていました

## v0.10.0

### 直った不具合

- **ROCm環境で文字起こしが必ず失敗していた。** whisper.cppが未取得のとき、
  配布物に入っていない公式Whisper(torch依存)へ落ちていました
- **セットアップのモデル取得が終わらなかった。** ダウンロードが完了しても
  ボタンが「取得中…」のまま固まっていました
- **「おすすめ設定を適用」がASRエンジンを固定していた。** GPUがあっても
  遅いエンジンが使われ続ける原因になっていました
- **APIキーのファイルをメモ帳で作ると落ちていた。** 説明文(全角)が残ったまま
  保存すると、キーが読めないだけで済まず例外になっていました
- **ユーザー名が日本語のWindows機で顔検出が効いていなかった。** 例外が出ないまま
  検出0件になるため、中央クロップとの区別がつきませんでした
- **Windowsで字幕フォントが実質選べなかった。** 候補が Noto Sans JP 固定で、
  Windowsには標準搭載されていないフォントでした
- **AppImageでffmpeg未導入のとき、書き出しが無案内で失敗していた。**
  導入手順を出すようにしました

### 変わったこと

- **効いていない設定4件をUIから削除しました。** 相槌除外の有効/無効・字幕モード・
  字幕の最大行数・指示語置換の有効/無効。いずれもバックエンドが読んでおらず、
  切り替えても結果が変わらない状態でした
- **話者分離はONNX版のみになりました。** torchを使うpyannoteは配布物では
  一度も動かなかったため削除しました。**HuggingFaceトークンは不要です**
- ASRエンジンの選択肢から transformers版Whisper を削除しました(同じ理由)

### 開発側

- CIでテストが一度も走っていませんでした。3OSで pytest・フロント・cargo test・E2E を
  回すワークフローを新設しました
- `.deb` / `.AppImage` / `.dmg` は一度もインストール・起動されずに配布されていました。
  リリース時に3OSとも実機で検証します
- Linux/macOSの配布物が whisper.cpp のMIT再配布条件を満たしていませんでした。
  Apache-2.0(話者分離の埋め込みモデル)の本文も未同梱でした。どちらも同梱しました

---

Windowsは署名していないため、SmartScreenの警告で「詳細情報 → 実行」を選ぶ必要があります。
同梱しているソフトウェアのライセンス表記は [THIRD_PARTY_LICENSES.md](THIRD_PARTY_LICENSES.md)、
アプリ内では **設定タブ → オープンソースソフトウェア** から確認できます。
